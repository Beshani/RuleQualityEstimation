package edu.rit.goal.visitor;

import edu.rit.goal.metric.RuleMetric;

public class ExactRuleMetricListener extends RuleMetricListener {

	public ExactRuleMetricListener(RuleMetric metric) {
		super(metric);
	}

}
